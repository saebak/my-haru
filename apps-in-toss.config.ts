import { defineConfig } from '@apps-in-toss/web-framework/config';

export default defineConfig({
  appName: 'my-daily-todo',
  brand: {
    primaryColor: '#86B93F',
  },
  permissions: [],
  webBundleDir: 'dist',
});
