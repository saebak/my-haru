# AGENTS.md

## 프로젝트 목표

My Daily Todo는 앱인토스 WebView에서 실행할 개인용 할 일·반복 습관 관리 앱이다. 로컬 우선 사용성을 유지하면서 익명 사용자 기반 서버 백업을 제공하고, 알림·통계·광고를 단계적으로 추가한다.

## 기준 문서

- 제품·기능 요구사항의 단일 기준은 `doc/requirements.md`다.
- 현재 상태와 우선순위는 `doc/requirements-analysis.md`를 참고한다.
- 외부 공식 자료는 `doc/reference.md`에서 찾고, 확인 결과는 `doc/work-log.md`에 남긴다.
- 요구사항과 다른 구현이 필요하면 먼저 이유와 영향을 설명하고 문서를 갱신한다.

## 현재 상태

- React 18, TypeScript, Vite 6과 테스트·린트 환경이 구성되어 있다.
- 앱인토스 WebView SDK는 최신 3.x로 유지한다.
- TDS 패키지는 설치되어 있지만 필요한 컴포넌트를 도입하기 전까지 전역 Provider를 올리지 않는다.
- Todo·반복·수행 기록은 IndexedDB에 먼저 저장하고, 동기화 장애와 무관하게 핵심 기능이 동작한다.
- 앱인토스 `User.getAnonymousKey()`를 서버에서 HMAC 처리해 내부 `account_id`에 연결하며 별도 로그인 화면은 없다.
- Supabase 마이그레이션과 `account-sync` Edge Function v3가 배포돼 전체 스냅샷 백업·복원을 제공한다.
- 과거 닉네임 인증 프로토타입은 `supabase/archive/`에 비활성 상태로 보관하며 실행 경로에 포함하지 않는다.
- 기존 `index.html`, `styles.css`, `app.js` 정적 UI 목업은 `prototype/`에 보존되어 있다.
- 목업의 `localStorage`와 하드코딩 날짜는 프로덕션 구현으로 이전하지 않는다.

## 작업 원칙

1. 기존 사용자 변경을 보존하고 관련 없는 파일을 수정하지 않는다.
2. 기능을 구현할 때 관련 요구사항 ID와 수용 조건을 먼저 확인한다.
3. 할 일 제목, 메모, 인증 토큰, 운영 광고 ID를 로그나 분석 이벤트에 남기지 않는다.
4. 반복 날짜 계산은 UI에서 분리한 순수 함수로 구현한다.
5. 현재 시각, 시간대, ID 생성은 테스트에서 대체 가능하게 만든다.
6. 저장 작업은 원자적으로 처리하고 중복 제출을 막는다.
7. 알림·광고·분석 장애가 할 일 저장과 완료를 막지 않게 경계를 분리한다.
8. 외부 SDK 버전이나 정책을 추측하지 말고 공식 문서를 확인한다.
9. 완료한 작업과 검증 결과, 남은 제한을 `doc/work-log.md`에 기록한다.
10. 원문 익명 식별키와 세션 토큰은 로그나 데이터베이스에 저장하지 않고 서버에서 각각 HMAC·해시 처리한다.

## 구현 우선순위

1. 출시 버전 실제 Android·iOS 회귀 검증과 오류율 관찰
2. 서버 동기화 실패율·오류 코드 계측과 복원 검증
3. 전체 스냅샷을 증분 동기화·재시도 큐로 고도화
4. 계정 삭제·세션 폐기·충돌 해결 UX
5. 접근성·1,000개 항목 성능 검증
6. 알림과 통계
7. 광고·출시 운영

초기 범위에서 공유 목록, AI 추천, 외부 캘린더, 음성 입력, 위젯, 결제는 추가하지 않는다.

## 데이터 규칙

- 엔터티 ID는 안정적인 고유 식별자를 사용한다.
- 시각은 저장 시 UTC ISO 8601 형식을 사용하고, 날짜는 규칙 시간대의 `YYYY-MM-DD`로 다룬다.
- 별도 Habit 테이블 없이 Todo가 반복 정의를 소유하고 날짜별 수행·override는 TodoRecord로 분리한다.
- `Todo.type`은 단일·반복 저장 방식, `Todo.category`는 할 일·습관 표시 의미로 분리한다.
- 통계는 Phase 3에서 구현하며 주간 통계는 월요일에 시작하고 `skipped`는 달성률 분모에서 제외한다.
- 이 날짜부터 이후 수정은 기존 Todo를 전날 종료하고 같은 `seriesId`의 새 Todo를 생성해 과거 표시를 보존한다.
- 완료 상태와 `completedAt`은 항상 함께 변경한다.
- 삭제 항목은 `deletedAt`을 유지한 뒤 보존 기간 또는 동기화 완료 기준으로 정리한다.
- IndexedDB와 백업 파일에 스키마 버전을 둔다.
- 백업 가져오기는 전체 검증이 끝난 뒤 하나의 트랜잭션으로 기존 데이터를 전체 교체한다.

## UI와 접근성

- 모바일 세로 화면과 Safe Area를 우선한다.
- 주요 버튼과 입력은 충분한 터치 영역과 접근 가능한 이름을 가진다.
- 완료, 오류, 우선순위를 색상만으로 전달하지 않는다.
- 모달을 열 때 포커스를 이동하고 닫을 때 원래 위치로 복귀시킨다.
- 로딩, 빈 상태, 저장 실패, 오프라인 상태에 다음 행동을 안내한다.
- 앱인토스 내비게이션과 앱 내부 뒤로가기를 중복 제공하지 않는다.

## 테스트 기준

- 도메인 로직 변경에는 단위 테스트를 추가한다.
- 저장소 변경에는 IndexedDB 통합 테스트와 마이그레이션 fixture를 추가한다.
- 핵심 사용자 흐름은 실제 브라우저 E2E로 검증한다.
- Phase 1 반복 계산은 월말, 윤년, 자정과 한국 달력 경계를 포함한다. DST는 Phase 3 알림 설계에서 검증한다.
- 잘못된 백업으로 기존 데이터가 변경되지 않는지 검증한다.
- 1,000개 할 일에서 목록 조작 성능을 확인한다.
- 릴리스 전 Android와 iOS 실제 토스 앱에서 수동 테스트한다.

## 작업 완료 조건

- 요구사항의 정상·실패 흐름이 구현됐다.
- 관련 자동 테스트와 프로덕션 빌드가 통과한다.
- 접근성 이름, 포커스, 터치 영역을 확인했다.
- 비밀값과 사용자 입력이 로그·번들·분석에 포함되지 않는다.
- 변경된 사용법, 설계 판단, 제한사항을 문서에 반영했다.
- 검증하지 못한 항목은 완료로 표현하지 않고 구체적으로 기록한다.
