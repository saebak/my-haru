# 요구사항 분석

## 1. 분석 목적

`requirements.md`를 구현 가능한 작업 단위로 해석하고, 현재 정적 목업과 목표 제품 사이의 차이, 우선순위, 의존성을 명확히 한다. 이 문서는 요구사항을 대체하지 않으며 충돌 시 `requirements.md`가 우선한다.

## 2. 제품 범위 해석

제품은 다음 네 단계로 확장한다.

1. 개인 사용 MVP: 한 기기에서 할 일과 반복 습관을 오프라인으로 관리한다.
2. 계정·동기화: 인증을 확정하고 앱 삭제 후 복원, 서버 백업과 다중 기기 동기화를 제공한다.
3. 알림·사용성: 알림과 통계가 실제 습관 유지에 도움이 되는지 검증한다.
4. 출시·수익화: 앱인토스 검수, 배너 광고, 분석과 정산을 운영한다.

현재는 프로젝트 스캐폴딩과 1단계 UI 목업이 존재한다. React·TypeScript·Vite, 앱인토스 WebView SDK와 테스트 기반은 구성됐다. Supabase 클라이언트 연결은 준비돼 있지만 Phase 1 실행 경로에서는 사용하지 않으며 IndexedDB 데이터 정확성과 로컬 백업을 우선한다.

## 3. 현재 구현 평가

| 영역 | 현재 상태 | 관련 요구사항 | 판단 |
| --- | --- | --- | --- |
| 프로젝트 기반 | React 18, TypeScript, Vite 6, 앱인토스 SDK 3.4.0 구성 | 호환성 7.3 | 기반 구성 완료 |
| 모바일 화면 | 세로형 앱 셸과 Safe Area 스타일 존재 | `UX-001`, `UX-002` | 부분 충족 |
| 할 일 생성·수정 | 제목·메모·시간·우선순위 폼 존재 | `TODO-001`~`TODO-003` | 목업 수준 부분 충족 |
| 완료·복구 | 완료 토글과 화면 진행률 갱신 | `TODO-004`, `TODO-005` | 부분 충족 |
| 삭제 | 개별 삭제와 실행 취소 토스트 | `TODO-006` | 부분 충족 |
| 목록 | 오늘 할 일과 습관 표시 | `LIST-001` | 일부만 충족 |
| 필터·정렬 | 시간순 추가와 수동 순서 변경만 존재 | `LIST-002`, `LIST-003` | 미충족 |
| 반복 습관 | 요일·종료일 입력과 습관 완료 토글 | `HABIT-001`~`HABIT-008` | UI 목업만 존재 |
| 영속 저장 | 보존된 정적 목업에서만 `localStorage` 사용 | `DATA-001`~`DATA-003` | 실제 앱 저장소 미구현 |
| Supabase | 브라우저 클라이언트와 환경변수·연결 확인 구성 | `SYNC-001`~`SYNC-010` | Phase 2 보류, 현재 Todo 저장에 사용하지 않음 |
| 백업·복원 | 설정 행만 존재 | `DATA-004`~`DATA-006` | 미구현 |
| 알림 | 설정 행만 존재 | `NOTI-001`~`NOTI-008` | 미구현 |
| 로그인·동기화 | 없음 | `SYNC-001`~`SYNC-010` | 후속 단계 |
| 광고·분석 | 없음 | `AD-*`, `ANALYTICS-*` | 후속 단계 |
| 테스트 | Vitest·Testing Library 기반과 단위 테스트 8개 구성 | 8장 | 기반 완료, 도메인·저장소·E2E 확대 필요 |

현재 동작은 요구사항 충족 증거가 아니라 화면과 상호작용 방향을 검증하는 기준선으로 본다.

상세 구현 구조는 `architecture.md`, `database-design.md`, `api-design.md`를 따르며 미확정 정책은 `decision-log.md`에서 관리한다.

## 4. 1단계 MVP 핵심 작업

### P0: 데이터 유실 방지와 기본 CRUD

- React·TypeScript 프로젝트 구성
- Todo 모델과 생성·조회·수정·완료·미완료·삭제
- 완료 시각 기록과 복구 시 제거
- 저장 중 중복 제출 차단
- IndexedDB 스키마 버전과 저장소 구현
- 새로고침·재실행 보존 테스트

대응 요구사항: `TODO-001`~`TODO-006`, `TODO-008`, `DATA-001`~`DATA-003`, `DATA-007`

### P0: 반복 일정 정확성

- `Todo.type`으로 일반·반복 구분하고 반복 정의를 Todo에 포함
- 날짜별 수행 상태와 단일 날짜 override를 `TodoRecord`로 분리
- 매일·매주·특정 요일·사용자 지정 주기 계산
- 완료·미완료·건너뜀 기록
- 이 날짜만·이 날짜부터 이후 수정과 삭제
- 기존 Todo 종료와 같은 `seriesId`의 새 Todo 생성
- 월말·윤년·자정 경계 테스트

대응 요구사항: `HABIT-001`~`HABIT-005`, `HABIT-007`, `HABIT-008`

### P1: 목록 탐색과 데이터 관리

- 오늘·예정·완료 목록
- 캘린더 날짜별 일반·반복 Todo 표시
- 날짜·완료 여부·우선순위 필터
- 마감·우선순위·생성 시각 정렬
- 빈 상태와 지난/오늘 마감 구분
- 완료 항목 일괄 정리
- JSON 백업·검증·복원과 전체 초기화 확인

대응 요구사항: `TODO-007`, `LIST-001`~`LIST-005`, `DATA-004`~`DATA-006`

### P1: 품질과 접근성

- 앱인토스 내비게이션과 뒤로가기 정책 적용
- 충분한 터치 영역과 색상 외 상태 표기
- 로딩·저장 실패·오류 피드백
- 1,000개 항목 성능 점검
- Android·iOS 대표 환경 수동 테스트

대응 요구사항: `UX-001`~`UX-009`, 비기능 요구사항 7장, 테스트 요구사항 8장

## 5. 데이터 모델 해석

### Todo

일반 할 일과 반복되는 할 일의 내용·반복 정의를 함께 가진다. `type`은 `one_time | recurring`이며 반복 Todo는 `seriesId`, `revision`, 빈도·간격·요일·시작일·종료일을 가진다. 일반 Todo의 완료 상태 변경은 `status`와 `completedAt`을 하나의 원자 작업으로 저장한다.

### TodoRecord

반복 Todo series와 대상 날짜별 수행 상태를 저장한다. 같은 `seriesId + targetDate` 조합은 하나만 존재한다. 이 날짜만 수정·삭제하는 경우 같은 레코드의 override와 `hidden`을 사용한다. 조회 시 날짜에 유효한 Todo revision과 결합하므로 Todo 분할 시 record를 이동하지 않는다.

### Reminder

Phase 3에서 구현한다. 대상, 예약 시각, 시간대, 활성 여부와 발송 상태를 분리해 완료·삭제·시간 변경 시 예약을 취소하거나 갱신할 수 있게 한다.

## 6. 확정된 제품·기술 결정

| 항목 | 확정 결정 | 상태 |
| --- | --- | --- |
| 회원 진입 | Phase 2 시작 전 인증·닉네임 UX 재검토 | 동결 |
| 세션 보안 | 특정 기기 토큰 또는 Supabase Auth 방식으로 확정하지 않음 | 동결 |
| 다른 기기 복구 | 인증 방식 확정 전까지 지원하지 않음 | 보류 |
| Supabase Auth | 도입 보류 | 보류 |
| 앱인토스 SDK | 구현 시점의 최신 버전 사용 | 확정 |
| TDS | 필요한 컴포넌트가 생길 때만 Provider와 함께 도입 | 확정 |
| 주 시작 요일 | 월요일 | 확정 |
| 건너뜀 통계 | 달성률 분모에서 제외 | 확정 |
| 이 날짜부터 이후 반복 수정 | 기존 Todo 종료 후 같은 `seriesId`의 새 Todo를 생성 | 확정 |
| 반복 수정의 과거 표시 | 당시 규칙과 기록 기준으로 보존 | 확정 |
| 백업 가져오기 | 기존 데이터를 검증 후 전체 교체 | 확정 |
| 삭제 | 소프트 삭제 후 보존 기간 또는 동기화 완료 시 정리 | 확정 |
| 삭제 실행 취소 | 초기값 5초 | 확정 |
| 날짜 키 | 규칙 시간대의 `YYYY-MM-DD` 문자열 | 확정 |
| Phase 1 저장소 | Todo·반복·수행 기록은 IndexedDB에만 저장 | 확정 |
| 서버 백업·재설치 복원 | Phase 2에서 인증 방식 확정 후 Supabase 동기화로 구현 | 확정 |
| 반복 데이터 모델 | 별도 Habit 없이 Todo에 반복 정의 포함, 날짜별 상태는 TodoRecord | 확정 |
| 반복 수정 범위 | 이 날짜만, 이 날짜부터 이후 두 가지 | 확정 |
| 통계·알림 | Phase 3에서 구현 | 확정 |
| Phase 1 시간대 | `Asia/Seoul` 달력 문자열로 저장하고 기기 시간대 변경 시 재해석하지 않음 | 확정 |
| Phase 2 세부 설계 | 인증 결정 전까지 동결 | 확정 |

Phase 1에는 회원 진입과 세션 UI를 노출하지 않는다. Phase 2에서 인증·복구 방식을 확정할 때 닉네임은 인증 수단으로 사용하지 않는 원칙을 유지한다.

## 7. 주요 위험

### 날짜와 시간대

Phase 1은 `Asia/Seoul`의 달력 날짜와 `dueTime` 문자열로 계산·정렬하고 기기 시간대 변경 시 기존 값을 재해석하지 않는다. UTC 실제 시각 변환과 DST gap/fold 정책은 알림을 구현하는 Phase 3에서 결정한다.

### 저장소 전환

현재 `localStorage` 데이터 구조를 그대로 프로덕션 스키마로 간주하면 마이그레이션과 참조 무결성을 다루기 어렵다. IndexedDB 도입 시 목업 데이터는 개발용 fixture로만 취급한다.

### 반복 수정

이 날짜부터 이후 수정은 대상 Todo 종료, 이후 활성 revision 정리와 같은 `seriesId`의 `maxRevision + 1` Todo 생성으로 모델링한다. 이 날짜만 수정은 TodoRecord override로 처리하며 대상 날짜 이전 Todo와 record는 수정하지 않는다.

### 외부 SDK 의존

알림·로그인·광고 API와 정책은 변경될 수 있다. SDK 호출을 UI와 도메인 코드에 직접 퍼뜨리지 말고 어댑터에 모은다.

### 범위 확대

공유, 캘린더 연동, AI 추천, 위젯, 결제는 초기 제외 범위다. 핵심 기능과 리텐션을 검증하기 전에는 구현하지 않는다.

## 8. 단계별 완료 판단

### 개인 사용 MVP

- 한 기기에서 생성·완료·반복 기록이 재실행 후 유지됨
- 반복 경계와 백업·복원 테스트 통과
- 네트워크 없이 핵심 기능 사용 가능
- 데이터 유실 가능성이 있는 알려진 결함 없음

### 계정·서버 동기화

- 인증 방식을 확정하고 사용자별 데이터 격리 검증
- 기존 IndexedDB 데이터를 동일 UUID로 최초 업로드
- 앱 삭제·저장소 초기화 후 서버 데이터 복원
- 오프라인 재전송, 충돌, 삭제 동기화 검증

### 알림·사용성

- 실제 토스 앱에서 동의·철회·수신·딥링크 검증
- 통계와 알림을 1~2주 직접 사용해 효용 확인

### 출시·광고

- 테스트·운영 광고 설정 분리
- 광고 실패가 핵심 기능에 영향 없음
- 앱인토스 기능·디자인·보안 검수 통과
- 운영 지표와 정산 확인 가능
